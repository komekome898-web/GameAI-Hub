name: Orchestration label bootstrap
on: workflow_dispatch
permissions:
  issues: write
  contents: read
jobs:
  labels:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - env: { GH_TOKEN: "${{ github.token }}" }
        run: python scripts/orchestration_github.py labels
