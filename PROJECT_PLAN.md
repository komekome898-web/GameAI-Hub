# GameAI Hub プロジェクト計画

> **文書ステータス:** MVP 着手前の意思決定案
> **調査基準日:** 2026-08-25（UTC）
> **対象市場:** 日本語圏を主対象とする AI ゲーム開発支援
> **更新方針:** 重要な前提が変わった場合、変更理由・根拠・検証日を履歴に残す

## 0. エグゼクティブサマリー

GameAI Hub は「AI ツールを並べるメディア」ではなく、**ゲーム制作の要件から、検証可能な候補と概算費用、導入手順を得られる意思決定サービス**を目指す。

初期の勝ち筋は次の 4 点とする。

1. 公式情報に出典と確認日を付けた、ゲーム開発用途に特化した構造化データ
2. エンジン、制作物、予算、商用利用、API 要否を横断する検索・比較・スタック診断
3. 価格体系の違いを同一条件へ換算する料金シミュレーター
4. 比較結果から実作業へ進める、日本語の検証手順・失敗例・プロンプト

MVP では対象サービスを闇雲に増やさない。最初の 20〜30 サービスを一次情報で検証し、検索、比較、診断、料金試算の 4 体験を完成させる。記事はこのデータと独自ツールを補足する少数の「作業完了型ガイド」に限定する。

収益はアフィリエイトを中心とするが、推薦スコアと報酬を分離し、公式リンクを常に利用可能にする。収益のないサービスも同じ評価基準で掲載する。将来はスポンサー表示や B2B リードも選択肢だが、編集順位の販売は行わない。

---

## 1. 調査方法と前提

### 1.1 調査方法

この計画では以下を分けて扱う。

- **観測:** 2026-08-25 に日本語・英語の検索結果を確認し、競合のページ種別、訴求、更新表示を定性調査した。
- **一次情報:** Google Search Central、Schema.org、各サービス公式サイト・規約・料金ページを、実装時の仕様確認元とする。
- **仮説:** アクセス解析やユーザー調査がまだないため、需要・CVR・単価の数値は事実として断定せず、検証対象として管理する。
- **掲載条件:** 料金、無料枠、商用利用、API は公式の根拠を確認できた値だけ公開する。確認できなければ `unknown` とする。

調査時に見つかった日本語検索結果は、汎用メディアの「おすすめ N 選」、サービス自身のゲームメーカー、ゲーム開発特化の比較ページが中心だった。英語圏にはゲーム開発特化ディレクトリも存在する。この観測は機会の示唆であって、市場規模や検索需要を証明するものではない。

### 1.2 現時点の合理的な仮定

| ID | 仮定 | 検証方法 | 失敗時の対応 |
|---|---|---|---|
| A1 | 日本語ユーザーは一般的な AI 一覧より、Unity/Godot/Unreal 等との適合性を重視する | 検索・比較フィルター利用率、5〜8件のインタビュー | 上位フィルターと情報設計を利用実績順に変更 |
| A2 | 「無料」だけでなく商用利用可否と総費用が意思決定を左右する | 診断回答、料金計算完了率、外部リンク CTR | 価値の低い項目を縮小し、重視項目を追加 |
| A3 | 比較・診断の利用者は記事だけの訪問者より高い送客意図を持つ | 経路別 affiliate CTR と補助CV | 導線、診断質問、結果説明を再設計 |
| A4 | 20〜30件の高品質データで MVP の主要用途をカバーできる | ゼロ件検索率、比較候補不足率、サイト内検索語 | 不足カテゴリだけ追加調査 |
| A5 | 初期は日本語・JPY 表示が中心でよい | 国・言語・通貨の利用分布 | i18n と通貨換算を前倒し |

### 1.3 未取得データ

- Search Console、アクセス解析、ASP 成果データは未接続。
- キーワード検索量、広告単価、市場規模、想定 CVR は未検証。
- アフィリエイト提携可否・料率は ASP 審査後でなければ確定できない。
- 各ツールの実生成品質・処理時間は同一テスト条件で未計測。

したがって、売上予測は単一の金額で置かず、計測開始後にシナリオモデルを作る。

---

## 2. 市場調査

### 2.1 解決するジョブ

ユーザーの目的は「AI ツールを知ること」ではなく、次の作業を低リスクで完了することにある。

- ゲーム企画から必要な制作スタックを決める
- 限られた予算でコード、画像、3D、音、音声、テストを用意する
- 自分のエンジンと開発環境へ組み込めるか確認する
- 商用リリースで利用できるか根拠を確認する
- 月額だけでなく生成量・API 使用量を含む費用を予測する
- 候補を試し、採用・不採用の理由を説明可能にする

### 2.2 需要領域の優先度

市場規模の断定ではなく、ユーザーの作業頻度、比較の難しさ、更新価値、収益接続性で優先する。

| 優先 | 領域 | 主な判断軸 | MVP 方針 |
|---|---|---|---|
| P0 | AI コーディング | 対応IDE/言語、コード保持、料金、エージェント機能 | 掲載・比較・診断 |
| P0 | 2D画像・アセット | スタイル一貫性、編集、透過、商用条件、単価 | 掲載・比較・料金計算 |
| P0 | BGM・効果音・音声 | 尺、ループ、権利、生成量、多言語 | 掲載・比較・料金計算 |
| P0 | ゲームエンジン/生成 | エクスポート、所有権、拡張性、対象プラットフォーム | 掲載・比較・診断 |
| P1 | 3D・アニメーション | トポロジー、リグ、形式、品質、処理量 | 初期データ、詳細検証は次段階 |
| P1 | NPC AI | ランタイム費用、遅延、安全性、SDK、記憶 | 初期データ、料金モデルを拡張 |
| P1 | AIテスト・デバッグ | 対応エンジン、再現性、CI、価格 | 候補調査から開始 |
| P2 | 動画生成 | トレーラー、ストア素材、尺、権利 | ゲーム制作本体との接続を確認後 |

### 2.3 市場機会

1. **情報の鮮度:** 料金・プラン・規約変更に対し、記事本文より構造化レコードの方が更新・影響確認をしやすい。
2. **条件の非対称性:** 「無料」でも出力権利、クレジット、透かし、解像度、生成数が異なる。単純ランキングでは選べない。
3. **工程横断:** コード、アセット、音、NPC は別々に比較されがちだが、実際の制約はプロジェクト全体の予算・エンジン・権利条件で決まる。
4. **日本語の実装支援:** 海外サービスは公式情報が英語中心になりやすい。日本語で根拠と導入作業を結びつける余地がある。
5. **更新自体が価値:** 変更日、再確認予定、差分履歴を見せれば、再訪理由になる。

---

## 3. 競合分析

### 3.1 競合類型

| 類型 | 強み | 弱み/空白 | GameAI Hub の差別化 |
|---|---|---|---|
| 総合 AI ディレクトリ | 掲載数、カテゴリ網羅性、指名検索 | ゲーム固有の統合条件が浅い可能性 | エンジン、ファイル形式、ランタイム費用、商用条件まで構造化 |
| 「おすすめ N 選」記事 | 読みやすい、検索意図へ直結 | 比較軸・更新根拠・試算が限定的になりやすい | 出典付きレコードから比較表を生成し、検証方法も示す |
| ゲーム開発特化メディア/ディレクトリ | 専門読者、事例、コミュニティ | 日本語・料金換算・条件検索の空白 | 日本語、用途診断、JPY概算、更新履歴 |
| ツール提供企業の自社ガイド | 製品情報が詳しい、強い導入導線 | 比較の中立性と他社情報の網羅に限界 | 評価と報酬を分離し、根拠・弱点・代替候補を掲載 |
| SNS/動画/コミュニティ | 実演、速報性、体験談 | 再検索性、出典、更新性、比較が弱い | 構造データを基盤に実演と検証ログを蓄積 |

### 3.2 調査対象の例

2026-08-25 の探索で、比較対象候補として以下を確認した。ここでの列挙は品質保証や推奨ではない。

- 日本語: AI PICKS、AIツールナビ、テックジム、Summer Engine、CG研究所等のゲーム開発 AI 関連ページ
- 英語: The AI Game Dev の AI Tools Directory、汎用 AI ディレクトリ/比較サイト
- 直接競合: 「AI game development tools」「AI ゲーム開発 ツール 比較」で流入を狙う特化ページ
- 間接競合: Reddit、YouTube、X、Unity/Godot/Unreal コミュニティ、各サービス公式ドキュメント

競合ページの情報を転載・言い換えしない。競合はページ体験と情報欠落を理解するために使い、事実データは公式情報へ戻って確認する。

### 3.3 持続的な優位性

- サービス数ではなく、**同一条件で再現できるゲーム開発ベンチマーク**を蓄積する。
- 料金・規約の出典スナップショット、確認担当、変更履歴を持つ。
- 匿名化された診断条件と採用/不採用理由から推薦を改善する。
- 比較表、診断、計算機、ガイドが同じ中央データを参照する。
- 「誰に向くか」だけでなく「採用しない条件」を明示し、信頼を資産化する。

---

## 4. ターゲットユーザー分析

### 4.1 主要セグメント

| セグメント | 状況 | 重要課題 | 望む成果 | 主導線 |
|---|---|---|---|---|
| 初心者/ノーコード志向 | アイデアはあるが実装経験が少ない | 用語、選択肢過多、完成までの手順 | 最小構成で遊べる試作品 | 診断 → スタック → 初心者ガイド |
| 個人/インディー開発者 | 時間と予算が限られる | 品質一貫性、権利、費用予測 | 工程短縮と商用リリース | 用途検索 → 比較 → 料金計算 |
| エンジニア | Unity/Godot/Unreal 等を使用 | IDE/SDK/API適合、コード品質 | 既存工程への安全な統合 | 技術フィルター → 詳細 → 公式Docs |
| アーティスト/サウンド制作者 | 特定制作工程を効率化したい | 編集性、形式、権利、作風維持 | 再利用可能な素材 | カテゴリ比較 → ベンチマーク |
| 小規模チーム/企画担当 | 導入を説明・合意する必要 | 費用、規約、データ管理、再現性 | 比較根拠と予算表 | 比較保存/共有 → 試算 → 導入チェック |

### 4.2 意思決定軸

共通軸は価格、無料枠、商用利用、API、プラットフォーム、対応エンジン、日本語、出力形式、学習利用/データ保持、品質、速度、サポートとする。ただしカテゴリ固有軸を追加し、無意味な共通スコアへ過度に圧縮しない。

### 4.3 主要ユーザージャーニー

1. 検索またはガイドから用途ページへ到達
2. 前提条件（工程、エンジン、予算、商用、API）を入力
3. 適合候補と除外理由を見る
4. 2〜4件を比較し、根拠・確認日・弱点を確認
5. 利用量を入力し、月額/プロジェクト概算を確認
6. 公式サイトまたは開示済みアフィリエイトリンクへ進む
7. 試用結果を保存し、別工程のツール選定へ戻る

ログインは MVP の必須条件にせず、比較条件は URL とローカルストレージで保存可能にする。

---

## 5. 収益モデル

### 5.1 優先順位

1. **アフィリエイト:** 成果条件が明瞭なツールのみ。推薦評価と完全分離する。
2. **明示したスポンサー枠:** 十分な利用者と編集基準ができた後。自然順位へ混入させない。
3. **B2B リード/導入支援:** 問い合わせ需要を確認後。個人情報と利益相反の設計が前提。
4. **将来の有料機能:** チーム比較、予算エクスポート、変更アラート等。無料の基本選定を損なわない。

広告は速度・可読性・信頼を損ねやすいため、MVP の主要収益にはしない。

### 5.2 収益ファネル

`organic/session → tool_start → result_view → comparison → outbound_click → affiliate_click → conversion → approved_revenue`

最低限、以下をセグメント別に計測する。

- ランディングページ/検索意図
- カテゴリ、用途、エンジン、無料/商用フィルター
- 推薦順位と表示理由
- 公式リンク/アフィリエイトリンク
- 新規/再訪、デバイス
- 成果承認/否認（ASP から取得可能な範囲）

### 5.3 収益シナリオ式

実測前に恣意的な売上を置かず、次で計算する。

`月間承認収益 = 対象セッション × 結果到達率 × 外部CTR × 成果CVR × 承認率 × 平均報酬`

各変数に保守/基準/上振れを置き、週次で実測値へ置換する。報酬の高い案件へ順位を寄せず、**適合性を固定したうえで同等候補のCTA説明を改善**する。

---

## 6. 情報設計

### 6.1 サイトマップ

```text
/
├── /tools                         ツール検索
│   ├── /tools/[slug]              サービス詳細
│   └── /tools/compare?ids=...     比較（条件を共有可能）
├── /diagnosis                     開発スタック診断
│   └── /diagnosis/result?...      結果（noindex 方針を検討）
├── /calculator                    料金計算
├── /prompts                       プロンプト生成
├── /use-cases/[slug]              用途別の意思決定ページ
├── /categories/[slug]             カテゴリ概要
├── /guides/[slug]                 作業完了型ガイド
├── /methodology                   調査・評価・更新方法
├── /affiliate-disclosure          アフィリエイト開示
├── /updates                       変更履歴
├── /about
├── /privacy
└── /terms
```

### 6.2 ページ公開基準

- ツール詳細: 最低限の必須項目、一次情報、確認日、弱点が揃う。
- カテゴリ/用途: 3件以上の検証済み候補か、独自ツール/作業手順がある。
- 比較: 選択された実体のある候補だけを表示し、組み合わせ URL を大量に index させない。
- ガイド: 読者が具体的作業を完了でき、独自スクリーンショット/検証/データのいずれかを含む。
- 空・薄い絞り込みページ、サイト内検索結果、診断の個別結果は原則 `noindex`。

### 6.3 MVP の主要画面

- ホーム: 価値提案、用途選択、診断開始、更新済みデータの説明
- 検索: 複合フィルター、該当数、比較トレイ、確認日の警告
- 詳細: 結論、向く/向かない、仕様、料金、根拠、更新履歴、代替候補
- 比較: 差分のみ強調、unknown 表示、利用量を料金計算へ引き継ぐ
- 診断: 5〜7問、回答変更可能、適合/除外理由、確信度
- 料金計算: 通貨・税・従量課金前提、公式価格との差異注意、計算日時

---

## 7. 技術構成

リポジトリは計画時点で空のため、以下を初期案とする。実装前に ADR（Architecture Decision Record）で確定する。

### 7.1 推奨スタック

| 層 | 選択案 | 理由 |
|---|---|---|
| Web | Next.js（App Router）+ TypeScript | SSG/SSR、メタデータ、ルーティング、UIとAPIの共通型 |
| UI | Tailwind CSS + アクセシブルなヘッドレス部品 | 小規模で一貫したUI、不要JSを抑制 |
| DB | PostgreSQL | 構造化比較、履歴、関係、集計に適する |
| ORM/検証 | Drizzle ORM + Zod | スキーマの型安全性と入力/コンテンツ検証 |
| 検索 | 初期は PostgreSQL FTS/`pg_trgm` | 外部契約なしで開始。規模と品質に応じ専用検索へ移行 |
| コンテンツ | MDX + Git 管理、仕様値はDB/構造データ参照 | 編集レビューと中央データの両立 |
| テスト | Vitest、Testing Library、Playwright、axe | 単体・結合・E2E・アクセシビリティ |
| 監視 | Web Vitals、エラー監視、構造化ログ | 速度・失敗・利用を継続改善 |
| CI | GitHub Actions | lint、型、テスト、リンク、データ検証をPRで強制 |

外部ホスティング、DB、解析、監視は料金・契約・データ所在地を比較してから選ぶ。契約や課金はユーザー確認なしに行わない。

### 7.2 アーキテクチャ原則

- 表示ロジックと編集データを分離し、料金・URL・検証状態を単一ソースにする。
- 公開ページは可能な限り静的生成し、検索/比較だけを段階的に動的化する。
- 診断の推薦は説明可能なルールベースから始める。生成AIにランキングを決めさせない。
- 通貨換算値は参考値と明示し、取得時刻・レート元を保持する。
- JavaScript 無効でも主要本文とリンクを読めるようにする。
- Core Web Vitals の予算を CI と実測で監視する。

### 7.3 非機能目標（MVP）

- WCAG 2.2 AA を目標にキーボード、ラベル、コントラストを確認
- モバイル 360px から比較操作が可能
- p75 の LCP ≤ 2.5秒、INP ≤ 200ms、CLS ≤ 0.1 を目標（実ユーザーデータで判定）
- 主要検索・比較 API の p95 を観測し、目標値は初回負荷試験後に確定
- バックアップ復元手順、障害時の読み取り専用表示、監査ログを準備

---

## 8. データ構造

### 8.1 データ設計原則

- 「不明」と「非対応」と「対象外」を区別する。
- 現在値だけでなく有効期間と検証履歴を持つ。
- 一つの `pricing` 文字列に押し込まず、プラン、通貨、課金単位、上限を正規化する。
- 商用利用を boolean にしない。条件、対象プラン、帰属、出力所有権、根拠を持つ。
- `affiliate_url` はサービス別中央管理かつ履歴管理し、公開前に開示状態を検査する。
- 主観評価と確認可能な仕様を分ける。

### 8.2 中核エンティティ

```ts
type VerificationStatus = 'verified' | 'partially_verified' | 'unknown' | 'stale';
type Ternary = 'yes' | 'no' | 'conditional' | 'unknown' | 'not_applicable';

interface Service {
  id: string;
  slug: string;
  name: string;
  summaryJa: string;
  officialUrl: string;
  affiliateLinkId: string | null;
  status: 'draft' | 'published' | 'archived';
  categories: CategoryRef[];
  pricingSummary: string | null;
  freePlan: Ternary;
  commercialUse: Ternary;
  api: Ternary;
  supportedPlatforms: PlatformRef[];
  supportedEngines: EngineRef[];
  strengths: EvidenceBackedClaim[];
  weaknesses: EvidenceBackedClaim[];
  recommendedFor: UseCaseRef[];
  notRecommendedFor: UseCaseRef[];
  verificationStatus: VerificationStatus;
  lastVerifiedAt: string | null;
  nextReviewAt: string | null;
  sources: SourceRef[];
}
```

最低限要求された `name`, `category`, `official_url`, `affiliate_url`, `pricing`, `free_plan`, `commercial_use`, `api`, `supported_platforms`, `strengths`, `weaknesses`, `recommended_for`, `last_verified`, `sources` をすべて包含する。

### 8.3 関連テーブル

| テーブル | 主な項目 |
|---|---|
| `services` | 基本情報、公開状態、検証状態 |
| `categories` / `service_categories` | 階層カテゴリと多対多関係 |
| `pricing_plans` | plan、currency、billing_period、base_price、tax_included、effective_from/to |
| `pricing_allowances` | metric、included_quantity、overage_price、unit、reset_period |
| `capabilities` / `service_capabilities` | API、編集、形式、エンジン等の値と状態 |
| `commercial_terms` | commercial_use、attribution、output_ownership、training_use、conditions |
| `sources` | URL、title、publisher、source_type、accessed_at、content_hash、archive_ref |
| `claims` | field_path、value、source_id、quoted_fragment（短い根拠）、confidence |
| `verifications` | verified_at、reviewer、result、notes、next_review_at |
| `affiliate_links` | service_id、provider、destination、active_from/to、disclosure_required |
| `benchmarks` / `benchmark_runs` | 手順、入力、環境、出力指標、日付、再現性 |
| `recommendation_rules` | 条件、重み、除外、説明文、version |
| `events` | 匿名イベント、session_id、consent、properties、timestamp |
| `experiments` | 仮説、指標、開始/終了、結果、判断 |

### 8.4 推薦ロジック

1. 商用利用必須、対応エンジン、予算上限等のハード条件で除外する。
2. 用途適合、ワークフロー適合、費用予測可能性、検証鮮度等を重み付けする。
3. データ不足はゼロ点ではなく「不確実性」として表示し、上位化させない。
4. スコア内訳と除外理由をユーザーへ表示する。
5. アフィリエイト有無・料率は入力変数に含めない。
6. ルール版を結果に記録し、変更前後を評価できるようにする。

### 8.5 データ鮮度

- 料金、無料枠、商用条件: 原則30日ごとに再確認候補
- API/プラットフォーム: 原則60日ごと
- 基本説明: 原則90日ごと
- 公式ページ差分、リンクエラー、利用者報告: 期限前でも再確認

頻度は運用実績により変更する。自動差分は「変更候補」を作るだけで、規約の意味を自動確定しない。

---

## 9. SEO 戦略

### 9.1 方針

- プログラマティックSEOで組み合わせページを量産しない。
- 検索意図を「知る」「比較する」「計算する」「実装する」に分け、同じ意図の重複ページを統合する。
- 一次情報、検証手順、更新履歴、著者/確認者、評価方法を示す。
- カテゴリ名ではなく、ユーザーが完了したい作業を軸に内部リンクする。

### 9.2 技術 SEO

- ページ固有の title/description、自己参照 canonical、絶対URLの OGP
- `sitemap.xml` は公開・canonical・index可能ページのみ。`lastmod` は実質更新時だけ変更
- `robots.txt` でクロール制御しつつ、noindex はページメタで実施
- 404 は正しいステータスを返し、代替検索を提示。恒久移転のみ 301
- パンくずはUIと `BreadcrumbList` を一致させる
- サイト/組織情報には適切な `Organization`/`WebSite`、記事には実体のある `Article`
- 商品販売者でない比較ページへ不適切な Product/Review 星評価を付けない
- 構造化データは画面に見える内容と一致させる
- パラメータ比較・検索・診断結果の index 制御、canonical テスト
- 画像寸法、遅延読み込み、フォント、第三者スクリプトの性能予算

実装時は [Google Search Central](https://developers.google.com/search/docs) と [Schema.org](https://schema.org/) の最新仕様を再確認する。

### 9.3 コンテンツクラスター（初期）

1. AIゲーム開発ツールの選び方（ハブ）
2. Unity/Godot/Unreal 等の用途別スタック診断（データが揃った範囲）
3. 2Dアセット、3D、BGM、SFX、音声、コードの比較
4. 商用利用確認チェックリスト
5. 小規模ゲーム1本のAIツール費用計算
6. 同一課題ベンチマークと再現手順

各ページはツール検索・比較・計算へ接続し、ツール詳細から関連ガイドへ戻す。

### 9.4 計測と改善

- Search Console: impressions、clicks、CTR、average position、query/page/device
- Analytics: organic sessions、engaged session、tool start/complete、comparison、return rate
- 収益: outbound、affiliate CTR、conversion、approved revenue
- 品質: stale率、unknown率、根拠カバー率、リンクエラー、CWV

優先順位は、(a)表示が多くCTRが低い、(b)ツール開始が多く完了が低い、(c)外部クリック後の成果が低い、の順に原因を分解する。順位やCTRだけを改善して利用価値を下げない。

---

## 10. アフィリエイト戦略

### 10.1 編集と収益の分離

- `affiliate_links` だけでURLを管理し、コンポーネントや記事へ直接貼らない。
- アフィリエイトがないサービスにも同じ公式リンク CTA を提供する。
- 比較順位、長所短所、診断スコアへ報酬額を入力しない。
- アフィリエイトリンク付近とサイト共通ポリシーで関係を明示する。
- `rel="sponsored nofollow"` を基本とし、最新の検索エンジン方針を実装時に確認する。
- リダイレクトを使う場合も最終ドメインを明示し、クリック計測目的をプライバシー文書に記載する。

### 10.2 提携候補の選定基準

1. 対象ユーザーに実用価値がある
2. 公式規約・料金・サポート情報が確認できる
3. 誤認を招く訴求や不透明な請求がない
4. 成果条件、否認条件、Cookie期間、地域条件を記録できる
5. 代替サービスとの公平な比較ができる

ASP登録、契約、本人確認、銀行情報、課金が必要な作業は、候補・必要情報・期待効果を提示してユーザー承認を得る。

### 10.3 CTA の原則

- 「向いている条件」と「注意点」をCTA前に提示する。
- 「公式サイト」「無料で試す」等は実際の遷移先/条件と一致させる。
- 無料枠が確認できない場合「無料」と表記しない。
- 誇張した残数、架空割引、偽のカウントダウンは使わない。
- 公式リンクとアフィリエイトリンクが異なる場合、利用者が判別できるようにする。

---

## 11. 自動運営設計

### 11.1 パイプライン

```text
候補発見 → 公式ソース収集 → 差分検出 → 人間/担当エージェント確認
→ 構造化データ更新 → スキーマ/根拠/リンク検証 → プレビュー
→ レビュー → 公開 → 影響計測 → 実験記録
```

### 11.2 定期ジョブ

| 頻度案 | ジョブ | 出力 | 自動公開 |
|---|---|---|---|
| 日次 | 内外リンク、HTTP、リダイレクト、canonical、sitemap検査 | issue/レポート | 軽微な安全修正のみ |
| 日次 | 公式料金・規約ページの content hash 差分 | 再確認キュー | しない |
| 週次 | stale/unknown/根拠欠落、重複、構造化データ | 優先度付き品質レポート | しない |
| 週次 | Search Console/解析/収益ファネル | 改善候補 | しない |
| 週次 | CWV、バンドル、主要E2E | 回帰アラート | しない |
| 月次 | 新規サービス候補と競合差分 | 調査バックログ | 大量公開しない |
| 月次 | 上位ページの情報再検証 | 変更履歴 | レビュー後 |

### 11.3 問題処理テンプレート

```md
## 問題
観測値、影響範囲、発見日時
## 原因
確認済み事実と未確認仮説
## 改善案
期待効果、リスク、戻し方
## 実装
変更箇所、データ移行、開示
## テスト
自動/手動、モバイル、速度、リンク、SEO
## 結果
指標、期間、結論（採用/継続/撤回）
```

### 11.4 実験管理

- 仮説、一次指標、ガードレール、対象、期間、停止条件を開始前に記録
- 例: 「比較上部で用途別理由を示すと result→outbound CTR が上がる」
- ガードレール: 比較完了率、直帰、誤クリック、ページ速度、公式リンク利用率
- サンプル不足で勝敗を断定せず、探索結果として保存
- 実験IDと表示variantをイベントに含め、変更後に結果を追跡

---

## 12. リスクと対策

| リスク | 影響 | 予防/検知 | 対応 |
|---|---|---|---|
| 料金・規約の陳腐化 | 誤判断、信頼低下 | 確認日、期限、公式差分、更新キュー | 警告表示、該当値をunknown化、再確認 |
| 商用利用の誤解釈 | 法務/事業上の重大影響 | 原文根拠、条件付き型、法的助言でない旨 | 公開停止、訂正履歴、必要時は専門家確認 |
| AI生成の誤情報 | 品質低下 | AI出力を未確認データとして扱う | 一次情報で照合できなければ公開しない |
| アフィリエイト利益相反 | 信頼・検索評価低下 | スコア分離、開示、監査ログ | 順位監査、不適切案件停止 |
| 薄い大量ページ | インデックス/ブランド毀損 | 公開基準、noindex、重複チェック | 統合、削除、redirect |
| 外部API/解析への依存 | コスト/障害/データ流出 | 最小収集、抽象化、予算アラート | 機能縮退、代替、停止 |
| 個人情報/同意 | 規制・信頼リスク | 匿名イベント、保存期限、同意管理 | 削除/開示手順、法的変更は承認取得 |
| ベンダー終了/リンク切れ | 利用不能 | 自動リンク検査、archived状態 | 代替表示、履歴保持、index見直し |
| 通貨・税の誤差 | 試算誤認 | レート日時、税区分、丸め方表示 | 範囲表示、公式請求額優先の注意 |
| ベンチマーク偏り | 不公平な推薦 | 公開手順、複数入力、バージョン固定 | 再試験、限界を明記 |
| セキュリティ | 改ざん/漏えい | 最小権限、依存監査、CSP、秘密管理 | インシデント手順、キー失効、通知 |
| 運営能力超過 | 更新不能 | 掲載数上限、鮮度SLA、優先度 | 新規追加を止め既存品質を回復 |

法的に重要な利用規約・プライバシーポリシーの確定、Cookie同意要否、景品表示・広告表示への適合は、対象地域と計測構成が決まった時点で専門家確認を検討し、ユーザー承認を得る。

---

## 13. MVP

### 13.1 MVP の目的

「日本語の個人開発者が、ゲーム制作工程に合う候補を見つけ、根拠を確認し、比較と費用試算を経て試用先へ進める」ことを検証する。

### 13.2 スコープ

**含む**

- 20〜30件の検証済みサービス、主要6〜8カテゴリ
- 検索、無料/商用/API/エンジン/プラットフォーム絞り込み
- 最大4件の比較
- 5〜7問のルールベース・スタック診断
- 主要な月額/クレジット/生成単価の料金計算
- サービス詳細、評価方法、アフィリエイト開示、更新履歴
- SEO基盤、解析イベント、品質チェック、モバイル/アクセシビリティ
- 独自価値のある 3〜5 本のハブ/作業完了型ガイド

**含まない**

- ログイン、クラウド同期、コミュニティ投稿
- AIによる自動ランキング/自動公開
- 100件以上の一括掲載、多言語、大量カテゴリページ
- 有料プラン、スポンサー枠、外部サービスへの大量送信
- 法的判断や商用利用の保証

### 13.3 完了条件

- 必須項目が全公開サービスで埋まり、不明値は明示される
- 料金/無料/商用/APIの公開値に一次情報と確認日がある
- フィルター、比較、診断、料金計算の主要E2Eが通る
- アフィリエイト有無がランキング結果を変えないテストがある
- title、canonical、sitemap、robots、OGP、パンくず、404が検査済み
- モバイルとキーボード操作、axe の重大違反を確認済み
- Web Vitals と主要イベントを取得できる
- リンク切れ、期限切れデータ、根拠欠落をCI/定期処理で検出できる

### 13.4 MVP の成功指標（数値はベースライン取得後に設定）

- 検索/診断開始→結果到達率
- 結果→比較率、料金計算完了率
- 結果/比較→公式・アフィリエイト外部CTR
- 7日/30日再訪率
- ゼロ件率、unknown率、期限切れ率
- ユーザーからの誤情報報告率
- organic impressions/clicks（順位だけでなくタスク完了と併読）

---

## 14. 30日ロードマップ

1日を固定日付ではなく実作業日として数える。品質ゲートを通らなければ公開を遅らせ、ページ数で帳尻を合わせない。

### Days 1–3: 基盤と意思決定

- 本計画レビュー、成功条件と非対象の合意
- ADR: フレームワーク、DB、ホスティング候補、解析/同意方針
- 情報アーキテクチャ、デザイン原則、イベント命名規則
- サービス候補を用途カバレッジで選び、調査テンプレート作成
- 課金/契約/認証が必要な項目の承認リスト作成

**ゲート:** 技術選定の理由、撤退条件、データ必須項目がレビュー済み。

### Days 4–7: データと品質パイプライン

- DB/型/seed形式、source/claim/verification履歴を実装
- URL中央管理とアフィリエイト分離
- Zod 検証、重複、期限、根拠カバレッジ検査
- まず10件を一次情報で調査し、入力の詰まりを発見
- CI に lint、型、単体テスト、データ検査を追加

**ゲート:** サンプル10件について、必須仕様から公式根拠まで追跡可能。

### Days 8–12: 検索・詳細・比較

- レスポンシブな共通レイアウト、検索、フィルター
- 詳細ページと出典/確認日/unknown表示
- 比較トレイ、差分表示、URL共有
- methodology、affiliate disclosure、privacy draft
- メタデータ、canonical、パンくず、404の基盤

**ゲート:** モバイル/キーボードで検索→比較→公式リンクが完了。

### Days 13–17: 診断と料金計算

- 説明可能な推薦ルール、除外理由、ルール版管理
- 5〜7問の診断と回答変更
- 月額/年額/クレジット/従量課金の計算モデル
- 税、通貨、丸め、取得日時、不確実性の表示
- 診断→比較→料金計算への状態引継ぎ

**ゲート:** 境界値テストと「アフィリエイト有無で結果不変」テストが通る。

### Days 18–21: コンテンツと検証データ

- 追加10〜20件を一次情報で検証
- 3〜5本のハブ/作業完了型ガイドを作成
- 同一条件の小規模ベンチマークを最低1カテゴリで試行
- 内部リンク、用語、誤字、重複、引用を編集レビュー

**ゲート:** 公開候補すべてが掲載基準を満たし、薄いページがない。

### Days 22–25: SEO・性能・アクセシビリティ

- sitemap、robots、OGP、構造化データ、index制御
- Playwright E2E、axe、リンクチェッカー、構造化データ検査
- 360pxを含む実機相当表示、主要ブラウザ確認
- バンドル、画像、フォント、第三者スクリプト最適化
- Lighthouse と実測開始準備

**ゲート:** 重大なa11y/リンク/SEO不具合ゼロ。性能目標未達は原因と期限を記録。

### Days 26–28: 計測・運用

- 同意設計に沿って解析、Search Console 所有権確認準備
- ファネルイベント、affiliate click、エラー、Web Vitals
- 日次/週次の品質ジョブとアラート
- バックアップ/復元、ロールバック、インシデント手順
- 実験テンプレートと最初の仮説を登録

**ゲート:** テストイベントが重複せず、個人情報を不要に収集しない。

### Days 29–30: リリース判定

- 全公開前チェックとデータ再確認
- クロール、モバイル、速度、リンク、表記の最終確認
- 小規模公開またはプレビューで観測
- 問題→原因→改善→再テスト
- 次の30日バックログを実測/不足データ順に並べる

**ゲート:** 完了条件を満たし、未解決の高リスク項目がない。大量公開はしない。

### 30日後の次優先候補

データがない現時点では、次の作業を断定しない。以下のルールで一つを選ぶ。

1. 誤情報/期限切れ/障害があれば品質修復
2. 診断・比較の完了率が低ければUX改善
3. ゼロ件や不足用途が多ければ該当データ追加
4. 外部CTRは高いが成果が低ければ期待値と遷移先を監査
5. 高価値ページの表示はあるがCTRが低ければ検索スニペットと意図適合を改善
6. 十分な利用がなければ独自ベンチマーク/配布可能な計算ツールを強化

---

## 15. ユーザー承認・外部情報が必要な事項

以下は計画上の候補であり、自動契約・課金・登録しない。

- ドメイン、ホスティング、DB、メール、監視、解析等の有料契約
- ASP/個別アフィリエイトプログラム登録、本人確認、銀行情報
- Search Console/解析/ASPの認証情報と権限付与
- Cookie同意管理や法務レビューの有料サービス
- 有料APIを使う料金/為替/生成品質テスト
- プライバシーポリシー、利用規約、広告表示等の法的に重要な確定
- 100件規模のデータ投入、多言語化、大量ページ公開
- ユーザーデータを外部AI/APIへ送る機能

承認依頼時は、目的、費用、代替、データ取扱い、解約/撤退条件を提示する。

---

## 16. 参考情報と再検証ルール

### 16.1 実装時に優先する情報源

1. 各サービスの公式 pricing、terms、license、privacy、API docs、changelog
2. エンジン/プラットフォームの公式ドキュメント
3. Google Search Central、Schema.org、W3C/WAI 等の標準・公式ガイダンス
4. 再現可能な自社テスト
5. 補助的に第三者レビュー（仕様の最終根拠にはしない）

### 16.2 調査起点リンク

- [Google Search Central](https://developers.google.com/search/docs)
- [Schema.org](https://schema.org/)
- [W3C Web Content Accessibility Guidelines](https://www.w3.org/WAI/standards-guidelines/wcag/)
- [The AI Game Dev - AI Tools Directory](https://theaigamedev.com/ai-tools/)

検索で発見した個別メディアは競合観測の起点に留め、掲載仕様の根拠には使わない。各サービスの具体的な料金・無料枠・商用条件は、データ投入日に公式ソースを再確認する。

---

## 17. 変更履歴

| 日付 | 変更 | 理由 | 結果 |
|---|---|---|---|
| 2026-08-25 | 初版作成 | 実装前に市場、利用者、収益、情報、技術、運用、MVPを合意可能にするため | レビュー待ち。コード実装は未着手 |
