import { describe,expect,it,vi } from 'vitest';import { buildSubId,sanitizeEventProperties,track } from '@/lib/analytics';import { ANALYTICS_CONSENT_KEY } from '@/lib/analytics-consent';
describe('analytics',()=>{it('emits outbound event without personal data',()=>{const listener=vi.fn();window.addEventListener('gameai:event',listener);track('outbound_click',{service:'cursor',page:'/compare',placement:'table'});expect(listener).toHaveBeenCalledOnce();expect(JSON.stringify(listener.mock.calls[0])).not.toMatch(/email|name|ip_address/)});it('drops unknown and event-inappropriate properties while preserving sub_id',()=>{const unsafe={service:'elevenlabs',page:'/tools/elevenlabs',placement:'primary',sub_id:'elevenlabs__tools__primary',email:'private@example.com',budget:'secret'};expect(sanitizeEventProperties('affiliate_click',unsafe)).toEqual({service:'elevenlabs',page:'/tools/elevenlabs',placement:'primary',sub_id:'elevenlabs__tools__primary'})});it('queues production events before gtag initializes',()=>{vi.stubEnv('NODE_ENV','production');const target=window as typeof window&{dataLayer?:unknown[];gtag?:unknown};delete target.gtag;target.dataLayer=[];localStorage.setItem(ANALYTICS_CONSENT_KEY,'accepted');track('builder_step',{step:1,page:'/builder'});expect(target.dataLayer).toContainEqual(['event','builder_step',{step:1,page:'/builder'}]);vi.unstubAllEnvs()});it('does not forward production events without consent or after rejection',()=>{vi.stubEnv('NODE_ENV','production');const target=window as typeof window&{dataLayer?:unknown[];gtag?:unknown};delete target.gtag;target.dataLayer=[];localStorage.removeItem(ANALYTICS_CONSENT_KEY);track('builder_step',{step:1,page:'/builder'});localStorage.setItem(ANALYTICS_CONSENT_KEY,'rejected');track('builder_step',{step:2,page:'/builder'});expect(target.dataLayer).toEqual([]);vi.unstubAllEnvs()});it('builds stable subtracking identifiers',()=>expect(buildSubId('cursor','/compare','top cta')).toBe('cursor__-compare__top-cta'))});
