'use client';
import type { Service } from '@/lib/schema';import { buildSubId,track } from '@/lib/analytics';import { getOutboundUrl } from '@/lib/services';
export function OutboundLink({service,page,placement='primary'}:{service:Service;page:string;placement?:string}){const affiliate=Boolean(service.affiliateUrl);return <div className="cta-wrap"><a className="button" href={getOutboundUrl(service)} target="_blank" rel={affiliate?'sponsored nofollow noopener':'noopener'} onClick={()=>{const props={service:service.slug,page,placement,sub_id:buildSubId(service.slug,page,placement)};track('outbound_click',props);if(affiliate)track('affiliate_click',props)}}>{service.freePlan==='yes'?'無料枠を公式サイトで確認':'公式サイトを見る'} <span aria-hidden>↗</span></a>{affiliate&&<small>このリンクはアフィリエイトリンクです。</small>}</div>}
