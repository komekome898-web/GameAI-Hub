import type { Metadata } from 'next';
import Link from 'next/link';
import { notFound } from 'next/navigation';
import { getGuide, guides } from '@/data/guides';
export const dynamicParams=false;
export function generateStaticParams(){return guides.map(({slug})=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const guide=getGuide(slug);if(!guide)return{};const path=`/guides/${slug}/`;return{title:guide.title,description:guide.description,alternates:{canonical:path},openGraph:{title:`${guide.title} | GameAI Hub`,description:guide.description,url:path}}}
export default async function GuidePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const guide=getGuide(slug);if(!guide)notFound();return <article className="page-shell"><header className="page-head"><p className="eyebrow">PRACTICAL WORKFLOW · {guide.queryFamily}</p><h1>{guide.title}</h1><p className="lead">{guide.description}</p><dl className="requirement-summary"><div><dt>対象</dt><dd>{guide.audience}</dd></div><div><dt>最初の到達点</dt><dd>{guide.outcome}</dd></div><div><dt>最終確認</dt><dd>{guide.lastVerified}</dd></div></dl><Link className="button" href="/project">この条件でProject Planを作る</Link></header><section><p className="section-label">WORKFLOW</p><h2>成果物と完了条件</h2><div className="stage-results">{guide.steps.map(step=><details className="stage-decision" key={step.title} open><summary><strong>{step.title}</strong><small>{step.objective}</small></summary><div className="stage-decision-body"><h3>作るもの</h3><ul>{step.deliverables.map(item=><li key={item}>{item}</li>)}</ul><h3>完了条件</h3><ul>{step.doneWhen.map(item=><li key={item}>{item}</li>)}</ul><p className="critical-checks"><strong>避けること:</strong> {step.pitfall}</p></div></details>)}</div></section><section><p className="section-label">SHIP CHECK</p><h2>公開前まで維持する確認</h2><ul>{guide.checks.map(check=><li key={check}>{check}</li>)}</ul></section><section><p className="section-label">PRIMARY SOURCES</p><h2>参照した公式情報</h2><p>製品仕様は更新されます。実装時はリンク先の最新版を確認してください。</p><ul>{guide.sources.map(source=><li key={source.url}><a href={source.url} rel="noopener" target="_blank">{source.label} ↗</a></li>)}</ul></section><section className="result-next"><h2>自分のゲームへ落とし込む</h2><p>ゲーム形式、対象プラットフォーム、予算、必要な素材を確認し、不要な工程を除いた計画を作ります。</p><Link className="button" href="/project">Project Generatorを開く</Link></section></article>}
