import type { ReactNode } from 'react';

/** Promotions are opt-in at the exact production step; article layout never injects them globally. */
export default function ArticlesLayout({children}:{children:ReactNode}){return children}
