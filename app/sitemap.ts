import type { MetadataRoute } from 'next';import { getServices } from '@/lib/services';import { site } from '@/lib/site';export const dynamic='force-static';export default function sitemap():MetadataRoute.Sitemap{const paths=['','/tools','/compare','/methodology','/affiliate-disclosure','/privacy'];return [...paths.map(path=>({url:`${site.url}${path}`,lastModified:'2026-08-25'})),...getServices().map(s=>({url:`${site.url}/tools/${s.slug}`,lastModified:s.lastVerified}))]}
