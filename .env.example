# Required for production builds. Use the deployed HTTPS origin without a trailing slash.
# Local development can keep the example value; Vercel Preview builds may override it.
NEXT_PUBLIC_SITE_URL=https://gameai-hub.example
